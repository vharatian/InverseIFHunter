fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
h2>=4.1.0  # For HTTP/2 support (optional but recommended)
orjson>=3.9.0  # Fast JSON parsing (3-10x faster than stdlib)
openai>=1.10.0
python-multipart>=0.0.6
pydantic>=2.5.0
python-dotenv>=1.0.0
aiofiles>=23.2.1
sse-starlette>=2.0.0

# Redis for session storage
redis>=5.0.0

# Google Drive API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# PostgreSQL + ORM
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29.0
greenlet>=3.0
alembic>=1.13

# Task queue (replaces custom hunt_worker loop)
arq>=0.25

# Structured logging
structlog>=24.1

# Metrics
prometheus-fastapi-instrumentator>=6.1

# Config (already used via agentic_reviewer but not declared)
PyYAML>=6.0
